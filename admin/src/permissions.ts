const pluginPermissions = {
  see: [{ action: 'plugin::media-twins.see', subject: null }],
  configure: [{ action: 'plugin::media-twins.configure', subject: null }],
  reassign: [{ action: 'plugin::media-twins.reassign', subject: null }],
  delete: [{ action: 'plugin::media-twins.delete', subject: null }],
};

export default pluginPermissions;
